import CircleLoader from "../../CircleLoader";

export default function ButtonBigIconSecondary({
	text,
	dsiabled,
	seeter,
	fetching,
	width,
	icon,
}){
	return(
		<button 
		onClick={setter}
		disabled ={disabled}
		className={'${
			disabled
			}
	);
}